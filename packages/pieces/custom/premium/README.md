# premium

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build premium` to build the library.

## Running unit tests

Run `nx test premium` to execute the unit tests via [Jest](https://jestjs.io).
