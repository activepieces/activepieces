{
  "en": {
    "Docling": "Docling",
    "Convert documents (PDF, DOCX, PPTX, images, HTML, …) to Markdown, docling-document JSON, HTML, DocTags and plain text via a docling-serve v1 API (self-hosted or Docling for IBM watsonx).": "Convert documents (PDF, DOCX, PPTX, images, HTML, …) to Markdown, docling-document JSON, HTML, DocTags and plain text via a docling-serve v1 API (self-hosted or Docling for IBM watsonx).",
    "Docling Serve": "Docling Serve",
    "A docling-serve v1 API server — self-hosted, or Docling for IBM watsonx (same API).": "A docling-serve v1 API server — self-hosted, or Docling for IBM watsonx (same API).",
    "Service URL": "Service URL",
    "Base URL of the docling-serve instance, e.g. http://localhost:5001 — or your Docling-for-IBM-watsonx service URL.": "Base URL of the docling-serve instance, e.g. http://localhost:5001 — or your Docling-for-IBM-watsonx service URL.",
    "API Key": "API Key",
    "The server's DOCLING_SERVE_API_KEY value, sent as the X-Api-Key header. Leave empty for an unauthenticated local server.": "The server's DOCLING_SERVE_API_KEY value, sent as the X-Api-Key header. Leave empty for an unauthenticated local server.",
    "Output Format": "Output Format",
    "OCR": "OCR",
    "Run OCR on the pages (server default: on).": "Run OCR on the pages (server default: on).",
    "Table Mode": "Table Mode",
    "Page Range": "Page Range",
    "Optional 1-based [start, end] page window, e.g. [1, 20].": "Optional 1-based [start, end] page window, e.g. [1, 20].",
    "Image Mode": "Image Mode",
    "Execution": "Execution",
    "Timeout (seconds)": "Timeout (seconds)",
    "Overall deadline for async conversions (5–3600).": "Overall deadline for async conversions (5–3600).",
    "Markdown": "Markdown",
    "Markdown + document model (JSON)": "Markdown + document model (JSON)",
    "All (md, json, html, text, doctags)": "All (md, json, html, text, doctags)",
    "Fast": "Fast",
    "Accurate (TableFormer)": "Accurate (TableFormer)",
    "Placeholder": "Placeholder",
    "Embedded (base64 in output)": "Embedded (base64 in output)",
    "Referenced (URLs)": "Referenced (URLs)",
    "Auto (async — recommended)": "Auto (async — recommended)",
    "Sync (fast documents only; server caps at ~120 s)": "Sync (fast documents only; server caps at ~120 s)",
    "Check Health": "Check Health",
    "Checks the docling-serve connection (GET /health and /version). Use as a pre-flight step.": "Checks the docling-serve connection (GET /health and /version). Use as a pre-flight step.",
    "Verifies the docling-serve endpoint is reachable and returns its component versions.": "Verifies the docling-serve endpoint is reachable and returns its component versions.",
    "File": "File",
    "The document to convert (PDF, DOCX, PPTX, XLSX, images, HTML, …).": "The document to convert (PDF, DOCX, PPTX, XLSX, images, HTML, …).",
    "Convert File": "Convert File",
    "Converts an uploaded document with a docling-serve v1 API and returns the requested output formats (Markdown by default).": "Converts an uploaded document with a docling-serve v1 API and returns the requested output formats (Markdown by default).",
    "Converts a document file to Markdown (or docling-document JSON / HTML / DocTags / plain text) using a docling-serve v1 service. Use for “parse/extract/read this document”. Text out; slow for very large documents (default deadline 10 minutes).": "Converts a document file to Markdown (or docling-document JSON / HTML / DocTags / plain text) using a docling-serve v1 service. Use for “parse/extract/read this document”. Text out; slow for very large documents (default deadline 10 minutes).",
    "URL": "URL",
    "Public URL of the document to convert (PDF, DOCX, …). ZIP archives are not supported.": "Public URL of the document to convert (PDF, DOCX, …). ZIP archives are not supported.",
    "Convert URL": "Convert URL",
    "Downloads and converts a document from a public URL with a docling-serve v1 API and returns the requested output formats.": "Downloads and converts a document from a public URL with a docling-serve v1 API and returns the requested output formats.",
    "Converts a document at a public URL to Markdown (or other formats) via docling-serve. Use when the source is a link rather than an uploaded file.": "Converts a document at a public URL to Markdown (or other formats) via docling-serve. Use when the source is a link rather than an uploaded file.",
    "Submit Job": "Submit Job",
    "Submits a document conversion as an async job and returns its task id (no waiting). Pair with “Get Result”.": "Submits a document conversion as an async job and returns its task id (no waiting). Pair with “Get Result”.",
    "Starts a background document conversion on docling-serve and returns a task id. Non-idempotent (each call starts a new job). Use with “Get Result” for long-running conversions.": "Starts a background document conversion on docling-serve and returns a task id. Non-idempotent (each call starts a new job). Use with “Get Result” for long-running conversions.",
    "Document to convert (leave one of file/url empty).": "Document to convert (leave one of file/url empty).",
    "Document URL (leave one of file/url empty).": "Document URL (leave one of file/url empty).",
    "Get Result": "Get Result",
    "Checks an async conversion job (submitted by “Submit Job”). Returns the converted document when finished, or the current job status otherwise.": "Checks an async conversion job (submitted by “Submit Job”). Returns the converted document when finished, or the current job status otherwise.",
    "Polls a docling-serve job by task id. Idempotent; safe to re-run (e.g. via a workflow retry) until the job finishes.": "Polls a docling-serve job by task id. Idempotent; safe to re-run (e.g. via a workflow retry) until the job finishes.",
    "Task ID": "Task ID",
    "The task_id returned by “Submit Job”.": "The task_id returned by “Submit Job”.",
    "Wait (seconds)": "Wait (seconds)",
    "Long-poll wait on the server, 0–30.": "Long-poll wait on the server, 0–30.",
    "Chunk": "Chunk",
    "Splits a document from an upload or URL into chunks with a docling-serve v1 API (hybrid or hierarchical chunker) and returns the chunk texts.": "Splits a document from an upload or URL into chunks with a docling-serve v1 API (hybrid or hierarchical chunker) and returns the chunk texts.",
    "Splits a document into text chunks via docling-serve using the hybrid or hierarchical chunker. Idempotent — safe to re-run. Use for RAG / knowledge-base ingestion.": "Splits a document into text chunks via docling-serve using the hybrid or hierarchical chunker. Idempotent — safe to re-run. Use for RAG / knowledge-base ingestion.",
    "Chunker": "Chunker",
    "Hybrid (paragraph-level, default)": "Hybrid (paragraph-level, default)",
    "Hierarchical (by document structure)": "Hierarchical (by document structure)"
  }
}
