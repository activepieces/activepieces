# pieces-bilig

## Building

Run `turbo run build --filter=@activepieces/piece-bilig` to build the library.

## Testing

Run `turbo run test --filter=@activepieces/piece-bilig` to test the WorkPaper helper logic.
