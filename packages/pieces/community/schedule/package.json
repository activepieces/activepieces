{
  "name": "@activepieces/piece-schedule",
  "version": "0.1.16",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json && cp package.json ../../../../dist/packages/pieces/community/schedule/",
    "lint": "eslint 'src/**/*.ts'"
  }
}
