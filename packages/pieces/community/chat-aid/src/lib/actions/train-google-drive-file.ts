import { createAction, Property } from '@activepieces/pieces-framework';

export const trainGoogleDriveFile = createAction({
  // auth: check https://www.activepieces.com/docs/developers/piece-reference/authentication,
  name: 'trainGoogleDriveFile',
  displayName: 'train google drive file',
  description: '',
  props: {},
  async run() {
    // Action logic here
  },
});
