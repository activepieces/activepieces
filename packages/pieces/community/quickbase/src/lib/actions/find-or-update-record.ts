import { createAction, Property } from '@activepieces/pieces-framework';

export const find-or-update-record = createAction({
  // auth: check https://www.activepieces.com/docs/developers/piece-reference/authentication,
  name: 'find-or-update-record',
  displayName: 'find-or-update-record',
  description: '',
  props: {},
  async run() {
    // Action logic here
  },
});
