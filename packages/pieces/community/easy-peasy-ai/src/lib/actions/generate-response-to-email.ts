import { createAction, Property } from '@activepieces/pieces-framework';

export const generateResponseToEmail = createAction({
  // auth: check https://www.activepieces.com/docs/developers/piece-reference/authentication,
  name: 'generateResponseToEmail',
  displayName: 'Generate Response to Email',
  description: '',
  props: {},
  async run() {
    // Action logic here
  },
});
