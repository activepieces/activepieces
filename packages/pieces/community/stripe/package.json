{
  "name": "@activepieces/piece-stripe",
  "version": "0.6.4",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json && cp package.json ../../../../dist/packages/pieces/community/stripe/",
    "lint": "eslint 'src/**/*.ts'"
  }
}
