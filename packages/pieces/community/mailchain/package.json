{
  "name": "@activepieces/piece-mailchain",
  "version": "0.1.2",
  "dependencies": {
    "@mailchain/sdk": "^0.31.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.lib.json && cp package.json ../../../../dist/packages/pieces/community/mailchain/",
    "lint": "eslint 'src/**/*.ts'"
  }
}
