import { createAction, Property } from '@activepieces/pieces-framework';

export const find-notebook-or-notebook-comment = createAction({
  // auth: check https://www.activepieces.com/docs/developers/piece-reference/authentication,
  name: 'find-notebook-or-notebook-comment',
  displayName: 'find-notebook-or-notebook-comment',
  description: '',
  props: {},
  async run() {
    // Action logic here
  },
});
