# pieces-linkly

Linkly (https://linklyhq.com) is a URL shortener with click tracking, branded domains, retargeting pixels and geo/device redirect rules.

## Building

Run `turbo run build --filter=@activepieces/piece-linkly` to build the library.
