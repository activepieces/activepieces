export enum RightSideBarType {
  NONE = 'none',
  PIECE_SETTINGS = 'piece-settings',
  VERSIONS = 'versions',
  RUNS = 'runs',
}

export enum ChatDrawerSource {
  TEST_FLOW = 'test-flow',
  TEST_STEP = 'test-step',
}
