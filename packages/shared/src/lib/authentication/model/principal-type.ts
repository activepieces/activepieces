export enum PrincipalType {
    USER = 'USER',
    WORKER = 'WORKER',
    SERVICE = 'SERVICE',
    UNKNOWN = 'UNKNOWN',
}
export enum EndpointScope {
    PLATFORM = 'PLATFORM',
    PROJECT = 'PROJECT',
}