// Stub for removed billing feature
import React from 'react';

interface PurchaseExtraFlowsDialogProps {
  children?: React.ReactNode;
}

export const PurchaseExtraFlowsDialog: React.FC<PurchaseExtraFlowsDialogProps> = ({ children }) => {
  // Disabled - EE feature removed
  return <>{children}</>;
};
