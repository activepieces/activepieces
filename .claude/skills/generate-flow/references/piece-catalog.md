# ActivePieces Piece Catalog

> Auto-generated by `scripts/build-piece-catalog.ts`
> Last updated: 2026-02-16

This catalog lists available pieces with their triggers, actions, and key input properties.
Use this to determine valid `pieceName`, `triggerName`/`actionName`, and `input` fields when generating flow JSON.

---

## Table of Contents

- [Activepieces Platform](#activepieces-platform) — `@activepieces/piece-activepieces`
- [Amazon S3](#amazon-s3) — `@activepieces/piece-amazon-s3`
- [Assembled](#assembled) — `@activepieces/piece-assembled`
- [Azure OpenAI](#azure-openai) — `@activepieces/piece-azure-openai`
- [BambooHR](#bamboohr) — `@activepieces/piece-bamboohr`
- [Captain-data](#captain-data) — `@activepieces/piece-captain-data`
- [CSV](#csv) — `@activepieces/piece-csv`
- [customer.io](#customer-io) — `@activepieces/piece-customer-io`
- [Data Mapper](#data-mapper) — `@activepieces/piece-data-mapper`
- [Datadog](#datadog) — `@activepieces/piece-datadog`
- [Date Helper](#date-helper) — `@activepieces/piece-date-helper`
- [Delay](#delay) — `@activepieces/piece-delay`
- [Docusign](#docusign) — `@activepieces/piece-docusign`
- [Dust](#dust) — `@activepieces/piece-dust`
- [ElevenLabs](#elevenlabs) — `@activepieces/piece-elevenlabs`
- [Files Helper](#files-helper) — `@activepieces/piece-file-helper`
- [Flow Helper](#flow-helper) — `@activepieces/piece-flow-helper`
- [Fragment](#fragment) — `@activepieces/piece-fragment`
- [FTP/SFTP](#ftp-sftp) — `@activepieces/piece-sftp`
- [GitHub](#github) — `@activepieces/piece-github`
- [Gmail](#gmail) — `@activepieces/piece-gmail`
- [Google Calendar](#google-calendar) — `@activepieces/piece-google-calendar`
- [Google Docs](#google-docs) — `@activepieces/piece-google-docs`
- [Google Drive](#google-drive) — `@activepieces/piece-google-drive`
- [Google Forms](#google-forms) — `@activepieces/piece-google-forms`
- [Google Sheets](#google-sheets) — `@activepieces/piece-google-sheets`
- [Google Slides](#google-slides) — `@activepieces/piece-google-slides`
- [HTTP](#http) — `@activepieces/piece-http`
- [HTTP (OAuth2)](#http-oauth2-) — `@activepieces/piece-http-oauth2`
- [Human Input](#human-input) — `@activepieces/piece-forms`
- [Intercom](#intercom) — `@activepieces/piece-intercom`
- [JSON](#json) — `@activepieces/piece-json`
- [Linear](#linear) — `@activepieces/piece-linear`
- [Math Helper](#math-helper) — `@activepieces/piece-math-helper`
- [Metabase](#metabase) — `@activepieces/piece-metabase`
- [Notion](#notion) — `@activepieces/piece-notion`
- [OpenAI](#openai) — `@activepieces/piece-openai`
- [PDF](#pdf) — `@activepieces/piece-pdf`
- [Queue](#queue) — `@activepieces/piece-queue`
- [Saleor](#saleor) — `@activepieces/piece-saleor`
- [Schedule](#schedule) — `@activepieces/piece-schedule`
- [Slack](#slack) — `@activepieces/piece-slack`
- [SMTP](#smtp) — `@activepieces/piece-smtp`
- [Snowflake](#snowflake) — `@activepieces/piece-snowflake`
- [Storage](#storage) — `@activepieces/piece-store`
- [Sub Flows](#sub-flows) — `@activepieces/piece-subflows`
- [Tables](#tables) — `@activepieces/piece-tables`
- [Text Helper](#text-helper) — `@activepieces/piece-text-helper`
- [Webhook](#webhook) — `@activepieces/piece-webhook`

---

## Activepieces Platform

- **Package**: `@activepieces/piece-activepieces`
- **Auth**: CustomAuth
- **Description**: Open source no-code business automation

### Actions

#### `create_project` — Create Project
Create a new project

**Props:**
  - `display_name` (ShortText, required) — Display Name

#### `list_project` — List Projects
List all projects

**Props:**
  _(no props)_

#### `update_project` — Update Project
Update a project

**Props:**
  - `id` (ShortText, required) — Id
  - `display_name` (ShortText, required) — Display Name
  - `notify_status` (StaticDropdown, required) — Notify Status
  - `team_members` (Number, required) — Team Members

---

## Amazon S3

- **Package**: `@activepieces/piece-amazon-s3`
- **Auth**: CustomAuth
- **Description**: Scalable storage in the cloud

### Triggers

#### `new_file` — New or Updated File
Triggers when you add or update a file in your bucket. The bucket/folder you choose must not contain more than 10,000 files.

**Props:**
  - `markdown` (MarkDown) — markdown
  - `folderPath` (ShortText) — Folder Path

### Actions

#### `decrypt-pgp-file` — Decrypt PGP File
Decrypt a PGP encrypted file from S3 using a private key stored in AWS Secrets Manager

**Props:**
  - `key` (ShortText, required) — S3 File Key
  - `secretArn` (ShortText, required) — Secret ARN
  - `passphraseArn` (ShortText) — Passphrase ARN
  - `secretsManagerRegion` (ShortText) — Secrets Manager Region

#### `deleteFile` — Delete File
Deletes an existing file.

**Props:**
  - `key` (ShortText, required) — Key

#### `generate-signed-url` — Generate signed URL
Generate a signed URL for a file in a s3 bucket

**Props:**
  - `key` (ShortText, required) — Key
  - `expiresIn` (Number, required) — Expires In (minutes)

#### `list-files` — List Files
List all files from an S3 bucket folder/prefix.

**Props:**
  - `prefix` (ShortText) — Folder path
  - `maxKeys` (Number) — Maximum Files

#### `moveFile` — Move File
Move a File to Another Folder

**Props:**
  - `fileKey` (ShortText, required) — File Key
  - `folderKey` (ShortText, required) — Folder Key

#### `read-file` — Read File
Read a file from S3 to use it in other steps

**Props:**
  - `key` (ShortText, required) — Key

#### `upload-file` — Upload File
Upload an File to S3

**Props:**
  - `file` (File, required) — File
  - `fileName` (ShortText) — File Name
  - `acl` (StaticDropdown) — ACL
  - `type` (ShortText) — Content Type

---

## Assembled

- **Package**: `@activepieces/piece-assembled`
- **Auth**: Unknown
- **Description**: Workforce management platform for scheduling and forecasting

### Triggers

#### `OOO_status_changed` — OOO Status Changed
Triggers on approval/rejection of OOO.

**Props:**
  _(no props)_

#### `new_OOO_request` — New OOO Request
Triggers when a new OOO request is created.

**Props:**
  _(no props)_

#### `schedule_updated` — Schedule Updated
Triggers when user schedule is modified.

**Props:**
  _(no props)_

### Actions

#### `add_shift` — Add Shift on Assembled
Add a new shift to a user\

**Props:**
  - `agent_id` (ShortText, required) — Agent ID
  - `start_time` (DateTime, required) — Start Time
  - `end_time` (DateTime, required) — End Time
  - `shift_type` (StaticDropdown) — Shift Type
  - `activity_type_id` (ShortText, required) — Activity Type ID
  - `notes` (LongText) — Notes

#### `custom_graphql` — Custom GraphQL
Perform a custom GraphQL query

**Props:**
  - `query` (LongText, required) — Query
  - `variables` (Object) — Parameters

#### `delete_OOO` — Delete OOO Request
Cancel/delete a OOO request.

**Props:**
  - `OOO_id` (ShortText, required) — OOO ID

#### `get_user_schedule` — Get User Schedule
Retrieves user\

**Props:**
  - `user_id` (ShortText, required) — User ID
  - `start_date` (DateTime, required) — Start Date
  - `end_date` (DateTime, required) — End Date

#### `OOO` — Create OOO Request
Create an Out of Office request in Assembled.

**Props:**
  - `mock_mode` (Checkbox) — Mock Mode
  - `user_id` (ShortText, required) — User ID
  - `start_date` (DateTime, required) — Start Date
  - `end_date` (DateTime, required) — End Date
  - `activity_type_id` (ShortText, required) — Activity Type ID
  - `all_day` (Checkbox) — All Day Event
  - `reason` (LongText) — Reason

#### `update_OOO` — Update OOO Request
Updates an existing OOO request.

**Props:**
  - `OOO_id` (ShortText, required) — OOO ID
  - `start_date` (DateTime) — Start Date
  - `end_date` (DateTime) — End Date
  - `status` (StaticDropdown) — Status
  - `reason` (LongText) — Reason
  - `user_id` (ShortText, required) — User ID
  - `activity_type_id` (ShortText, required) — Activity Type ID

---

## Azure OpenAI

- **Package**: `@activepieces/piece-azure-openai`
- **Auth**: CustomAuth
- **Description**: Powerful AI tools from Microsoft

### Actions

#### `ask_gpt` — Ask GPT
Ask ChatGPT anything you want!

**Props:**
  - `deploymentId` (ShortText, required) — Deployment Name
  - `prompt` (LongText, required) — Question
  - `temperature` (Number) — Temperature
  - `maxTokens` (Number, required) — Maximum Tokens
  - `topP` (Number) — Top P
  - `frequencyPenalty` (Number) — Frequency penalty
  - `presencePenalty` (Number) — Presence penalty
  - `memoryKey` (ShortText) — Memory Key
  - `roles` (Json) — Roles

---

## BambooHR

- **Package**: `@activepieces/piece-bamboohr`
- **Auth**: Unknown

### Triggers

#### `reportFieldChanged` — Report Field Changed
Triggers when a specific field in a BambooHR report changes for any employee.

**Props:**
  - `reportId` (ShortText, required) — Report ID
  - `fieldToMonitor` (Dropdown, required) — Field to Monitor

---

## Captain-data

- **Package**: `@activepieces/piece-captain-data`
- **Auth**: CustomAuth

### Actions

#### `getJobResults` — Get job results
Get all results for a specific job

**Props:**
  - `job` (Dropdown, required) — Job

#### `launchWorkflow` — Launch a workflow
Inputs are the starting point of your workflow.

**Props:**
  - `jobName` (ShortText) — Job name
  - `inputs` (Json) — Inputs
  - `steps` (Json) — Steps
  - `delay` (Number) — Delay (seconds)

---

## CSV

- **Package**: `@activepieces/piece-csv`
- **Auth**: None
- **Description**: Manipulate CSV text

### Actions

#### `convert_csv_to_json` — Convert CSV to JSON
This function reads a CSV string and converts it into JSON array format.

**Props:**
  - `csv_text` (LongText, required) — CSV Text
  - `has_headers` (Checkbox, required) — Does the CSV have headers?
  - `delimiter_type` (StaticDropdown, required) — Delimiter Type

#### `convert_json_to_csv` — Convert JSON to CSV
This function reads a JSON array and converts it into CSV format.

**Props:**
  - `markdown` (MarkDown) — markdown
  - `json_array` (Json, required) — JSON Array
  - `delimiter_type` (StaticDropdown, required) — Delimiter Type

---

## customer.io

- **Package**: `@activepieces/piece-customer-io`
- **Auth**: CustomAuth
- **Description**: Create personalized journeys across all channels with our customer engagement platform.

### Actions

#### `create_event` — Create Event
Create an event in Customer.io

**Props:**
  - `customer_identifier` (ShortText, required) — Customer identifier
  - `event_name` (ShortText, required) — Event Name
  - `body_type` (StaticDropdown) — parameters
  - `body` (DynamicProperties, required) — Body

---

## Data Mapper

- **Package**: `@activepieces/piece-data-mapper`
- **Auth**: None
- **Description**: tools to manipulate data structure

### Actions

#### `advanced_mapping` — Advanced Mapping
Map data from one format to another

**Props:**
  - `mapping` (Json, required) — Mapping

---

## Datadog

- **Package**: `@activepieces/piece-datadog`
- **Auth**: CustomAuth
- **Description**: Cloud monitoring and analytics platform

### Actions

#### `sendMultipleLogs` — Send Multiple logs
Send your logs to your Datadog platform over HTTP.

**Props:**
  - `body` (Json, required) — Logs

#### `sendOneLog` — Send One log
Send one log to your Datadog platform over HTTP.

**Props:**
  - `message` (ShortText, required) — Message
  - `ddsource` (ShortText) — DD Source
  - `ddtags` (ShortText) — DD Tags
  - `hostname` (ShortText) — Hostname
  - `service` (ShortText) — Service
  - `additionalProperties` (Json) — Additional Properties

---

## Date Helper

- **Package**: `@activepieces/piece-date-helper`
- **Auth**: None

### Actions

#### `add_subtract_date` — Add/Subtract Time
Add or subtract time from a date

**Props:**
  - `inputDate` (ShortText, required) — Input Date
  - `inputDateFormat` (StaticDropdown, required) — From Time Format
  - `outputFormat` (StaticDropdown, required) — To Time Format
  - `expression` (LongText, required) — Expression
  - `setTime` (ShortText) — Set Time To (24h format)
  - `useCurrentTime` (Checkbox) — Use Current Time

#### `date_difference` — Date Difference
Get the difference between two dates

**Props:**
  - `startDate` (ShortText, required) — Starting Date
  - `startDateFormat` (StaticDropdown, required) — Starting date format
  - `endDate` (ShortText, required) — Ending Date
  - `endDateFormat` (StaticDropdown, required) — Ending date format
  - `unitDifference` (StaticMultiSelectDropdown, required) — Unit

#### `extract_date_parts` — Extract Date Units
Extract date units ( year , month , day , hour , minute , second , day of week , month name ) from a date

**Props:**
  - `inputDate` (ShortText, required) — Input Date
  - `inputFormat` (StaticDropdown, required) — From Time Format
  - `unitExtract` (StaticMultiSelectDropdown, required) — Unit to Extract

#### `first_day_of_previous_month` — First Day of Previous Month
Get the date and time of the first day of the previous month

**Props:**
  - `time` (ShortText) — 24h Time
  - `currentTime` (Checkbox) — Use Current Time
  - `timeFormat` (StaticDropdown, required) — To Time Format

#### `format_date` — Format Date
Converts a date from one format to another

**Props:**
  - `inputDate` (ShortText, required) — Input Date
  - `inputFormat` (StaticDropdown, required) — From Time Format
  - `outputFormat` (StaticDropdown, required) — To Time Format

#### `get_current_date` — Get Current Date
Get the current date

**Props:**
  - `timeFormat` (StaticDropdown, required) — To Time Format

#### `last_day_of_previous_month` — Last Day of Previous Month
Get the date and time of the last day of the previous month

**Props:**
  - `time` (ShortText) — 24h Time
  - `currentTime` (Checkbox) — Use Current Time
  - `timeFormat` (StaticDropdown, required) — To Time Format

#### `next_day_of_week` — Next Day of Week
Get the date and time of the next day of the week

**Props:**
  - `weekday` (StaticDropdown, required) — Weekday
  - `time` (ShortText) — 24h Time
  - `currentTime` (Checkbox) — Use Current Time
  - `timeFormat` (StaticDropdown, required) — To Time Format

#### `next_day_of_year` — Next Day of Year
Get the date and time of the next day of the year

**Props:**
  - `month` (StaticDropdown, required) — Month
  - `day` (Number, required) — Day of Month
  - `time` (ShortText) — 24h Time
  - `currentTime` (Checkbox) — Use Current Time
  - `timeFormat` (StaticDropdown, required) — To Time Format

---

## Delay

- **Package**: `@activepieces/piece-delay`
- **Auth**: None
- **Description**: Use it to delay the execution of the next action

### Actions

#### `delayFor` — Delay For
Delays the execution of the next action for a given duration

**Props:**
  - `markdown` (MarkDown) — markdown
  - `unit` (StaticDropdown, required) — Unit
  - `delayFor` (Number, required) — Amount

#### `delay_until` — Delay Until
Delays the execution of the next action until a given timestamp

**Props:**
  - `markdown` (MarkDown) — markdown
  - `delayUntilTimestamp` (DateTime, required) — Date and Time

---

## Docusign

- **Package**: `@activepieces/piece-docusign`
- **Auth**: CustomAuth

### Actions

#### `getDocument` — Get document
Get document from a specific envelope

**Props:**
  - `accountId` (ShortText, required) — Account ID
  - `envelopeId` (ShortText, required) — Envelope ID
  - `documentId` (ShortText, required) — Document ID

#### `getEnvelope` — Get envelope
Get Docusign envelope

**Props:**
  - `accountId` (ShortText, required) — Account ID
  - `envelopeId` (ShortText, required) — Envelope ID

#### `listEnvelopes` — List envelopes
List / search envelopes

**Props:**
  - `accountId` (ShortText, required) — Account ID
  - `fromDate` (DateTime) — From date
  - `toDate` (DateTime) — To date
  - `searchText` (ShortText) — Search text
  - `status` (ShortText) — Status
  - `include` (Array) — Include (e.g. recipients)

---

## Dust

- **Package**: `@activepieces/piece-dust`
- **Auth**: CustomAuth
- **Description**: Secure messaging and collaboration

### Actions

#### `addFragmentToConversation` — Add fragment to conversation
Create a new content fragment in a conversation. Content fragments are pieces of information that can be inserted in conversations and are passed as context to assistants to when they generate an answer.

**Props:**
  - `conversationId` (ShortText, required) — Conversation ID
  - `fragment` (File, required) — Fragment
  - `fragmentName` (ShortText) — Fragment name

#### `createConversation` — Create conversation
Create a new conversation with a specific Dust assistant

**Props:**
  - `title` (ShortText) — Title
  - `query` (LongText) — Query
  - `fragment` (File) — Fragment
  - `fileId` (ShortText) — File ID
  - `fragmentName` (ShortText) — Fragment/file name

#### `getConversation` — Get existing conversation
Get an existing conversation

**Props:**
  - `conversationSid` (ShortText, required) — Conversation sID

#### `replyToConversation` — Reply to conversation
Send reply to existing conversation

**Props:**
  - `conversationId` (ShortText, required) — Conversation ID
  - `query` (LongText, required) — Query

#### `uploadFile` — Upload file
Upload file to be used in conversation

**Props:**
  - `file` (File, required) — File

#### `addDocument` — Add or update document
Insert a new document to a Data Source (or update an existing one)

**Props:**
  - `datasource` (ShortText, required) — Data Source name
  - `documentId` (ShortText, required) — Document ID
  - `content` (LongText, required) — Document content
  - `sourceUrl` (ShortText) — Source URL
  - `title` (ShortText) — Document title
  - `tags` (Array) — Tags

---

## ElevenLabs

- **Package**: `@activepieces/piece-elevenlabs`
- **Auth**: CustomAuth
- **Description**: AI Voice Generator & Text to Speech

### Actions

#### `elevenlabs-text-to-speech` — Text to Speech
Convert text to speech using Elevenlabs

**Props:**
  - `model` (Dropdown) — Model
  - `voice` (Dropdown, required) — Voice
  - `text` (LongText, required) — Text

---

## Files Helper

- **Package**: `@activepieces/piece-file-helper`
- **Auth**: None
- **Description**: Read file content and return it in different formats.

### Actions

#### `change_file_encoding` — Change File Encoding
Changes the encoding of a file

**Props:**
  - `inputFile` (File, required) — Source file
  - `inputEncoding` (StaticDropdown, required) — Source encoding
  - `outputFileName` (ShortText, required) — Output file name
  - `outputEncoding` (StaticDropdown, required) — Output encoding

#### `checkFileType` — Check file type
Check MIME type of a file and filter based on selected types

**Props:**
  - `file` (File, required) — File to Check
  - `mimeTypes` (StaticDropdown, required) — Select MIME Types

#### `createFile` — Create file
Create file from content

**Props:**
  - `content` (LongText, required) — Content
  - `fileName` (ShortText, required) — File name
  - `encoding` (StaticDropdown, required) — Encoding

#### `read_file` — Read File
Read a file from the file system

**Props:**
  - `file` (File, required) — File
  - `readOptions` (StaticDropdown, required) — Output format

#### `unzipFile` — Unzip File
Unzip compressed zip file

**Props:**
  - `file` (File, required) — Zip File
  - `maxResults` (Number) — Max Results
  - `usePassword` (Checkbox) — Use password
  - `passwordOptions` (DynamicProperties) — Password options
  - `password` (ShortText, required) — Password

#### `zipFiles` — Zip Files
Create compressed zip file from one or many files

**Props:**
  - `files` (Array) — Files
  - `file` (File, required) — File
  - `filePath` (ShortText, required) — File Path in zip
  - `outputFileName` (ShortText, required) — Name of zipped file
  - `usePassword` (Checkbox) — Use password
  - `passwordOptions` (DynamicProperties) — Password options
  - `password` (ShortText, required) — Password
  - `encryptionMethod` (StaticDropdown, required) — Encryption Method

---

## Flow Helper

- **Package**: `@activepieces/piece-flow-helper`
- **Auth**: None

### Actions

#### `failFlow` — Fail Flow
Fails the flow execution with a custom message.

**Props:**
  - `message` (LongText, required) — Error Message

#### `getRunId` — Get Run Info

**Props:**
  _(no props)_

#### `stopFlow` — Stop Flow
Stops the flow immediately this step is reached.

**Props:**
  _(no props)_

---

## Fragment

- **Package**: `@activepieces/piece-fragment`
- **Auth**: Unknown

### Triggers

#### `new_task` — New Task
Triggers when a new task is created.

**Props:**
  _(no props)_

#### `task_updated` — Task Updated
Triggers when a task is updated.

**Props:**
  _(no props)_

### Actions

#### `create_task` — Create Task
Creates a new task in Fragment.

**Props:**
  - `title` (ShortText, required) — Title
  - `url` (ShortText) — URL
  - `due_at` (DateTime) — Due Date
  - `assignee` (ShortText) — Assignee
  - `tags` (Array) — Tags
  - `custom_fields` (Object) — Custom Fields

#### `delete_task` — Delete Task
Deletes an existing task.

**Props:**
  - `task_uid` (ShortText, required) — Task UID

#### `get_task` — Get Task
Retrieves details of a specific task.

**Props:**
  - `task_uid` (ShortText, required) — Task UID

#### `list_tasks` — List Tasks
Retrieves a list of tasks from.

**Props:**
  - `status` (StaticDropdown) — Status
  - `assignee` (ShortText) — Assignee
  - `limit` (Number) — Limit

#### `update_task` — Update Task
Updates an existing task.

**Props:**
  - `task_uid` (ShortText, required) — Task UID
  - `title` (ShortText) — Title
  - `url` (ShortText) — URL
  - `due_at` (DateTime) — Due Date
  - `status` (StaticDropdown) — Status
  - `assignee` (ShortText) — Assignee
  - `tags` (Array) — Tags
  - `custom_fields` (Object) — Custom Fields

---

## FTP/SFTP

- **Package**: `@activepieces/piece-sftp`
- **Auth**: CustomAuth
- **Description**: Connect to FTP, FTPS or SFTP servers

### Triggers

#### `new_file` — New File
Trigger when a new file is created or modified.

**Props:**
  - `path` (ShortText, required) — Path
  - `ignoreHiddenFiles` (Checkbox) — Ignore hidden files

### Actions

#### `create_file` — Create File from Text
Create a new file in the given path

**Props:**
  - `fileName` (ShortText, required) — File Path
  - `fileContent` (LongText, required) — File content

#### `createFolder` — Create Folder
Creates a folder at given path.

**Props:**
  - `folderPath` (ShortText, required) — Folder Path
  - `recursive` (Checkbox) — Recursive

#### `deleteFile` — Delete file
Deletes a file at given path.

**Props:**
  - `filePath` (ShortText, required) — File Path

#### `deleteFolder` — Delete Folder
Deletes an existing folder at given path.

**Props:**
  - `folderPath` (ShortText, required) — Folder Path
  - `recursive` (Checkbox) — Recursive

#### `listFolderContents` — List Folder Contents
Lists the contents of a given folder.

**Props:**
  - `directoryPath` (ShortText, required) — Directory Path

#### `read_file_content` — Read File Content
Read the content of a file.

**Props:**
  - `filePath` (ShortText, required) — File Path

#### `renameFileOrFolder` — Rename File or Folder
Renames a file or folder at given path.

**Props:**
  - `information` (MarkDown) — information
  - `oldPath` (ShortText, required) — Old Path
  - `newPath` (ShortText, required) — New Path

#### `upload_file` — Upload File
Upload a file to the given path.

**Props:**
  - `fileName` (ShortText, required) — File Path
  - `fileContent` (File, required) — File content

---

## GitHub

- **Package**: `@activepieces/piece-github`
- **Auth**: OAuth2
- **Description**: Developer platform that allows developers to create, store, manage and share their code

### Actions

#### `add_labels_to_issue` — Add Labels to Issue
Adds labels to an existing issue.

**Props:**
  _(no props)_

#### `create_branch` — Create Branch
Creates a new branch on a repository.

**Props:**
  - `new_branch_name` (ShortText, required) — New Branch Name

#### `createCommentOnAIssue` — Create comment on a issue
Adds a comment to the specified issue (also works with pull requests)

**Props:**
  - `issue_number` (Number, required) — Issue number
  - `comment` (LongText, required) — Comment

#### `github_create_commit_comment` — Create Commit Comment
Creates a comment on a commit in a GitHub repository

**Props:**
  - `sha` (ShortText, required) — Commit SHA
  - `body` (LongText, required) — Comment Body
  - `path` (ShortText) — File Path
  - `position` (Number) — Position

#### `github_create_discussion_comment` — Create Discussion Comment
Creates a comment on a discussion in a GitHub repository

**Props:**
  - `discussion_number` (Number, required) — Discussion Number
  - `body` (LongText, required) — Comment Body

#### `github_create_issue` — Create Issue
Create Issue in GitHub Repository

**Props:**
  - `title` (ShortText, required) — Title
  - `description` (LongText) — Description

#### `github_create_pull_request_review_comment` — Create Pull Request Review Comment
Creates a review comment on a pull request in a GitHub repository

**Props:**
  - `pull_number` (Number, required) — Pull Request Number
  - `commit_id` (ShortText, required) — Commit SHA
  - `path` (ShortText, required) — File Path
  - `body` (LongText, required) — Comment Body
  - `position` (Number, required) — Position

#### `delete_branch` — Delete Branch
Deletes a branch from a repository.

**Props:**
  _(no props)_

#### `find_branch` — Find Branch
Finds a branch by name and returns its details.

**Props:**
  - `branch` (ShortText, required) — Branch Name

#### `find_issue` — Find Issue
Finds an issue based title.

**Props:**
  - `title` (ShortText, required) — Title
  - `state` (StaticDropdown, required) — State

#### `find_user` — Find User
Finds a user by their login name.

**Props:**
  - `username` (ShortText, required) — Username

#### `getIssueInformation` — Get issue information
Grabs information from a specific issue

**Props:**
  - `issue_number` (Number, required) — Issue Number

#### `lockIssue` — Lock issue
Locks the specified issue

**Props:**
  - `issue_number` (Number, required) — Issue Number

#### `rawGraphqlQuery` — Raw GraphQL query
Perform a raw GraphQL query

**Props:**
  - `query` (LongText, required) — Query
  - `variables` (Object) — Parameters

#### `unlockIssue` — Unlock issue
Unlocks the specified issue

**Props:**
  - `issue_number` (Number, required) — Issue Number

#### `update_issue` — Update Issue
Updates an existing issue.

**Props:**
  - `title` (ShortText) — Title
  - `body` (LongText) — Body
  - `state` (StaticDropdown) — State
  - `state_reason` (StaticDropdown) — State Reason

---

## Gmail

- **Package**: `@activepieces/piece-gmail`
- **Auth**: OAuth2
- **Description**: Email service by Google

### Triggers

#### `new_attachment` — New Attachment
Triggers when an email with an attachment arrives.

**Props:**
  - `subject` (ShortText) — Subject Contains
  - `filenameExtension` (ShortText) — File Extension

#### `new_conversation` — New Conversation
Triggers when a new email conversation (thread) begins

**Props:**
  - `subject` (ShortText) — Subject Contains
  - `maxAgeHours` (Number) — Maximum Age (Hours)

#### `gmail_new_email_received` — New Email
Triggers when new mail is found in your Gmail inbox

**Props:**
  _(no props)_

#### `new_label` — New Label
Triggers when a new label is created.

**Props:**
  _(no props)_

#### `new_labeled_email` — New Labeled Email
Triggers when a label is added to an email

**Props:**
  _(no props)_

### Actions

#### `create_draft_reply` — Create Draft Reply
Creates a draft reply to an existing email.

**Props:**
  - `reply_type` (StaticDropdown, required) — Reply Type
  - `body_type` (StaticDropdown, required) — Body Type
  - `body` (LongText) — Draft Reply Body
  - `include_original_message` (Checkbox, required) — Include Original Message
  - `sender_name` (ShortText) — Sender Name
  - `attachment` (File) — Attachment
  - `attachment_name` (ShortText) — Attachment Name

#### `gmail_get_mail` — Get Email
Get an email via Id.

**Props:**
  - `message_id` (ShortText, required) — Message ID

#### `gmail_get_thread` — Get Thread
Get a thread from your Gmail account via Id

**Props:**
  - `thread_id` (ShortText, required) — Thread ID

#### `reply_to_email` — Reply to Email
Reply to an existing email.

**Props:**
  - `reply_type` (StaticDropdown, required) — Reply Type
  - `body_type` (StaticDropdown, required) — Body Type
  - `body` (LongText, required) — Reply Body
  - `sender_name` (ShortText) — Sender Name
  - `attachment` (File) — Attachment
  - `attachment_name` (ShortText) — Attachment Name

#### `request_approval_in_mail` — Request Approval in Email
Send approval request email and then wait until the email is approved or disapproved

**Props:**
  - `receiver` (ShortText, required) — Receiver Email (To)
  - `cc` (Array) — CC Email
  - `bcc` (Array) — BCC Email
  - `subject` (ShortText, required) — Subject
  - `body` (ShortText, required) — Body
  - `reply_to` (Array) — Reply-To Email
  - `sender_name` (ShortText) — Sender Name
  - `from` (ShortText) — Sender Email
  - `in_reply_to` (ShortText) — In reply to

#### `gmail_search_mail` — Find Email
Find emails using advanced search criteria.

**Props:**
  - `content` (ShortText) — Email Content
  - `has_attachment` (Checkbox) — Has Attachment
  - `attachment_name` (ShortText) — Attachment Name
  - `after_date` (DateTime) — After Date
  - `before_date` (DateTime) — Before Date
  - `include_spam_trash` (Checkbox) — Include Spam & Trash
  - `max_results` (Number) — Max Results

#### `send_email` — Send Email
Send an email through a Gmail account

**Props:**
  - `receiver` (Array, required) — Receiver Email (To)
  - `cc` (Array) — CC Email
  - `bcc` (Array) — BCC Email
  - `subject` (ShortText, required) — Subject
  - `body_type` (StaticDropdown, required) — Body Type
  - `body` (ShortText, required) — Body
  - `reply_to` (Array) — Reply-To Email
  - `sender_name` (ShortText) — Sender Name
  - `from` (ShortText) — Sender Email
  - `attachments` (Array) — Attachments
  - `file` (File, required) — File
  - `name` (ShortText) — Attachment Name
  - `in_reply_to` (ShortText) — In reply to
  - `draft` (Checkbox, required) — Create draft

---

## Google Calendar

- **Package**: `@activepieces/piece-google-calendar`
- **Auth**: OAuth2
- **Description**: Get organized and stay on schedule

### Triggers

#### `new_or_updated_event` — New or Updated Event
Triggers when an event is added or updated

**Props:**
  - `expandRecurringEvent` (Checkbox, required) — Expand Recurring Event?

#### `event_cancelled` — Event Cancelled
Fires when an event is canceled or deleted.

**Props:**
  - `specific_event` (Checkbox) — Target Specific Event
  - `cancellation_reason` (StaticMultiSelectDropdown) — Cancellation Reasons

#### `event_ends` — Event Ends
Fires when an event ends.

**Props:**
  - `specific_event` (Checkbox) — Target Specific Event

#### `event_starts_in` — Event Start (Time Before)
Fires at a specified amount of time before an event starts (e.g., a reminder).

**Props:**
  - `specific_event` (Checkbox) — Target Specific Event
  - `time_value` (Number, required) — Time Before
  - `time_unit` (StaticDropdown, required) — Time Unit

#### `new_calendar` — New Calendar
Fires when a new calendar is created or becomes accessible.

**Props:**
  - `access_role_filter` (StaticMultiSelectDropdown) — Access Role Filter
  - `calendar_name_filter` (ShortText) — Calendar Name Filter
  - `exclude_shared` (Checkbox) — Exclude Shared Calendars

#### `new_event_matching_search` — New Event Matching Search
Fires when a new event is created that matches a specified search term.

**Props:**
  - `search_term` (ShortText, required) — Search Term
  - `event_types` (StaticMultiSelectDropdown) — Event Types
  - `search_fields` (StaticMultiSelectDropdown) — Search In Fields

#### `new_event` — New Event
Fires when a new event is created in a calendar.

**Props:**
  - `event_types` (StaticMultiSelectDropdown) — Event Types to Monitor
  - `search_filter` (ShortText) — Search Filter
  - `exclude_all_day` (Checkbox) — Exclude All-Day Events

### Actions

#### `google-calendar-add-attendees` — Add Attendees to Event
Add one or more person to existing event.

**Props:**
  - `eventId` (ShortText, required) — Event ID
  - `attendees` (Array, required) — Attendees

#### `addCalendarToCalendarlist` — Add Calendar to calendarList
Adds other people

**Props:**
  - `id` (ShortText, required) — Calendar Id

#### `create_google_calendar_event` — Create Event
Add Event

**Props:**
  - `title` (ShortText, required) — Title of the event
  - `start_date_time` (DateTime, required) — Start date time of the event
  - `end_date_time` (DateTime) — End date time of the event
  - `location` (ShortText) — Location
  - `description` (LongText) — Description
  - `attendees` (Array) — Attendees
  - `guests_can_modify` (Checkbox) — Guests can modify
  - `guests_can_invite_others` (Checkbox) — Guests can invite others
  - `guests_can_see_other_guests` (Checkbox) — Guests can see other guests
  - `send_notifications` (StaticDropdown, required) — Send Notifications
  - `create_meet_link` (Checkbox) — Create Google Meet Link

#### `create_quick_event` — Create Quick Event
Add Quick Calendar Event

**Props:**
  - `text` (LongText, required) — Summary

#### `delete_event` — Delete Event
Deletes an event from Google Calendar.

**Props:**
  - `eventId` (ShortText, required) — Event ID

#### `google_calendar_find_busy_free_periods` — Find Busy/Free Periods in Calendar
Finds free/busy calendar details from Google Calendar.

**Props:**
  - `calendar_ids` (MultiSelectDropdown, required) — Calendars
  - `start_date` (DateTime, required) — Start Time
  - `end_date` (DateTime, required) — End Time

#### `google_calendar_get_event_by_id` — Get Event by ID
Fetch event details by its unique ID from Google Calendar.

**Props:**
  - `event_id` (ShortText, required) — Event ID
  - `max_attendees` (Number) — Max Attendees
  - `time_zone` (ShortText) — Time Zone

#### `google_calendar_get_events` — Get all Events
Get Events

**Props:**
  - `event_types` (StaticMultiSelectDropdown, required) — Event types
  - `search` (ShortText) — Search Term
  - `start_date` (DateTime) — Date from
  - `end_date` (DateTime) — Date to
  - `singleEvents` (Checkbox, required) — Expand Recurring Event?

#### `update_event` — Update Event
Updates an event in Google Calendar.

**Props:**
  - `eventId` (ShortText, required) — Event ID
  - `title` (ShortText) — Title of the event
  - `start_date_time` (DateTime) — Start date time of the event
  - `end_date_time` (DateTime) — End date time of the event
  - `location` (ShortText) — Location
  - `description` (LongText) — Description
  - `attendees` (Array) — Attendees
  - `guests_can_modify` (Checkbox) — Guests can modify
  - `guests_can_invite_others` (Checkbox) — Guests can invite others
  - `guests_can_see_other_guests` (Checkbox) — Guests can see other guests

---

## Google Docs

- **Package**: `@activepieces/piece-google-docs`
- **Auth**: OAuth2
- **Description**: Create and edit documents online

### Triggers

#### `new-document` — New Document
Triggers when a new document is added to a specific folder(optional).

**Props:**
  _(no props)_

### Actions

#### `append_text` — Append text to google docs
Appends text to google docs

**Props:**
  - `text` (LongText, required) — Text to append
  - `documentId` (ShortText, required) — Document ID

#### `create_document_based_on_template` — Edit template file
Edit a template file and replace the values with the ones provided

**Props:**
  - `template` (ShortText, required) — Destination File
  - `values` (Object, required) — Variables
  - `images` (Object, required) — Images
  - `placeholder_format` (StaticDropdown, required) — Placeholder Format

#### `create_document` — Create Document
Create a document on Google Docs

**Props:**
  _(no props)_

#### `google-docs-find-document` — Find Document
Search for document by name.

**Props:**
  - `name` (ShortText, required) — Document Name
  - `createIfNotFound` (Checkbox) — Create a new document if not found?
  - `newDocumentProps` (DynamicProperties, required) — New Document Properties

#### `read_document` — Read Document
Read a document from Google Docs

**Props:**
  - `documentId` (ShortText, required) — Document ID

---

## Google Drive

- **Package**: `@activepieces/piece-google-drive`
- **Auth**: OAuth2
- **Description**: Cloud storage and file backup

### Triggers

#### `new_file` — New File
Trigger when a new file is uploaded.

**Props:**
  - `include_file_content` (Checkbox) — Include File Content

#### `new_folder` — New Folder
Trigger when a new folder is created or uploaded.

**Props:**
  _(no props)_

---

## Google Forms

- **Package**: `@activepieces/piece-google-forms`
- **Auth**: OAuth2
- **Description**: Receive form responses from Google Forms

### Triggers

#### `new_response` — New Response
Triggers when there is new response

**Props:**
  _(no props)_

---

## Google Sheets

- **Package**: `@activepieces/piece-google-sheets`
- **Auth**: Unknown
- **Description**: Create, edit, and collaborate on spreadsheets online

### Triggers

#### `google-sheets-new-or-updated-row` — New or Updated Row
Triggers when a new row is added or modified in a spreadsheet.

**Props:**
  - `info` (MarkDown) — info
  - `trigger_column` (Dropdown) — Trigger Column

#### `googlesheets_new_row_added` — New Row Added
Triggers when a new row is added to bottom of a spreadsheet.

**Props:**
  - `info` (MarkDown) — info

#### `new-spreadsheet` — New Spreadsheet
Triggers when a new spreadsheet is created.

**Props:**
  _(no props)_

#### `new-worksheet` — New Worksheet
Triggers when a worksheet is created in a spreadsheet.

**Props:**
  _(no props)_

### Actions

#### `clear_sheet` — Clear Sheet
Clears all rows on an existing sheet.

**Props:**
  - `headerRow` (Number, required) — Header Row Number

#### `copy-worksheet` — Copy Worksheet
Creates a new worksheet by copying an existing one.

**Props:**
  _(no props)_

#### `create-column` — Create Spreadsheet Column
Creates a new column in a specific spreadsheet.

**Props:**
  - `columnName` (ShortText, required) — Column Name
  - `columnIndex` (Number) — Column Index

#### `create-spreadsheet` — Create Spreadsheet
Creates a blank spreadsheet.

**Props:**
  - `title` (ShortText, required) — Title
  - `folder` (Dropdown) — Parent Folder

#### `create-worksheet` — Create Worksheet
Create a new blank worksheet with a title.

**Props:**
  - `title` (ShortText) — Title
  - `headers` (Array) — Headers

#### `delete_row` — Delete Row
Delete a specific row from the selected sheet.

**Props:**
  - `rowId` (Number, required) — Row Number

#### `delete-worksheet` — Delete Worksheet
Permanently delete a specific worksheet.

**Props:**
  _(no props)_

#### `export_sheet` — Export Worksheet
Download a worksheet as a CSV or TSV file.

**Props:**
  - `format` (StaticDropdown, required) — Export Format
  - `returnAsText` (Checkbox) — Return as Text

#### `find_row_by_num` — Get Single Row by ID
Retrieve a specific row using its unique ID.

**Props:**
  - `rowNumber` (Number, required) — Row Number
  - `headerRow` (Number, required) — Header Row Number

#### `find_rows` — Find Rows
Look up rows in a worksheet based on a column value.

**Props:**
  - `searchValue` (ShortText) — Search Value
  - `matchCase` (Checkbox, required) — Exact Match
  - `startingRow` (Number) — Starting Row
  - `numberOfRows` (Number) — Number of Rows
  - `headerRow` (Number, required) — Header Row
  - `useHeaderNames` (Checkbox) — Use Column Names

#### `find_spreadsheets` — Find Spreadsheet(s)
Find spreadsheet(s) by name.

**Props:**
  - `spreadsheet_name` (ShortText, required) — Spreadsheet Name
  - `exact_match` (Checkbox) — Exact Match

#### `find-worksheet` — Find Worksheet(s)
Finds a worksheet(s) by title.

**Props:**
  - `title` (ShortText, required) — Title
  - `exact_match` (Checkbox) — Exact Match

#### `format-row` — Format Row(s)
Format one or multiple rows in specific spreadsheet.

**Props:**
  - `startingRow` (Number, required) — Starting row
  - `endingRow` (Number) — Ending row
  - `bgColor` (ShortText) — Background Color
  - `textColor` (ShortText) — Text Color
  - `bold` (Checkbox) — Make text bold
  - `italic` (Checkbox) — Make text Italic
  - `strikethrough` (Checkbox) — Make text Strikethrough

#### `get-many-rows` — Get All Rows
Get all the rows from a specific sheet.

**Props:**
  _(no props)_

#### `get_next_rows` — Get next row(s)
Get next group of rows from a specifiec workheet

**Props:**
  - `startRow` (Number, required) — Start Row
  - `headerRow` (Number, required) — Header Row
  - `useHeaderNames` (Checkbox) — Use header names for keys
  - `markdown` (MarkDown) — markdown
  - `memKey` (ShortText, required) — Memory Key
  - `groupSize` (Number, required) — Group Size

#### `google-sheets-insert-multiple-rows` — Add Multiple Rows
Add multiple rows of data at once to a specific spreadsheet.

**Props:**
  - `input_type` (StaticDropdown, required) — Rows Data Format
  - `values` (DynamicProperties, required) — Values
  - `overwrite` (Checkbox) — Overwrite Existing Data?
  - `check_for_duplicate` (Checkbox) — Avoid Duplicates?
  - `check_for_duplicate_column` (DynamicProperties, required) — Duplicate Value Column
  - `as_string` (Checkbox) — As String
  - `headerRow` (Number, required) — Header Row Number

#### `insert_row` — Add Row
Add a new row of data to a specific spreadsheet.

**Props:**
  - `as_string` (Checkbox) — As String

#### `rename-worksheet` — Rename Worksheet
Rename specific worksheet.

**Props:**
  - `newName` (ShortText) — New Sheet Name

#### `update-multiple-rows` — Update Multiple Rows
Updates multiple rows in a specific spreadsheet.

**Props:**
  - `values` (DynamicProperties, required) — Values
  - `rowId` (Number, required) — Row Id
  - `as_string` (Checkbox) — As String
  - `headerRow` (Number, required) — Header Row

#### `update_row` — Update Row
Update the data in an existing row.

**Props:**
  - `row_id` (Number, required) — Row Number

---

## Google Slides

- **Package**: `@activepieces/piece-google-slides`
- **Auth**: OAuth2

### Actions

#### `generate_from_template` — Generate from template
Generate a new slide from a template

**Props:**
  - `template_presentation_id` (ShortText, required) — Template presentation ID
  - `placeholder_format` (StaticDropdown, required) — Placeholder Format
  - `table_data` (DynamicProperties, required) — Table Data
  - `title` (ShortText, required) — Presentation Title

#### `get_presentation` — Get Presentation
Get all slides from a presentation

**Props:**
  - `presentation_id` (ShortText, required) — Presentation ID

#### `refresh_sheets_charts` — Refresh Sheets Charts
Refresh all Google Sheets charts in the presentation

**Props:**
  - `presentation_id` (ShortText, required) — Presentation ID

---

## HTTP

- **Package**: `@activepieces/piece-http`
- **Auth**: None
- **Description**: Sends HTTP requests and return responses

### Actions

#### `send_request` — Send HTTP request
Send HTTP request

**Props:**
  - `url` (ShortText, required) — URL
  - `headers` (Object, required) — Headers
  - `queryParams` (Object, required) — Query params
  - `authFields` (DynamicProperties) — Authentication Fields
  - `username` (ShortText, required) — Username
  - `password` (ShortText, required) — Password
  - `token` (ShortText, required) — Token
  - `body_type` (StaticDropdown) — Body Type
  - `body` (DynamicProperties, required) — Body
  - `fieldName` (ShortText, required) — Field Name
  - `fieldType` (StaticDropdown, required) — Field Type
  - `textFieldValue` (LongText) — Text Field Value
  - `fileFieldValue` (File) — File Field Value
  - `response_is_binary` (Checkbox) — Response is Binary
  - `use_proxy` (Checkbox) — Use Proxy
  - `proxy_settings` (DynamicProperties, required) — Proxy Settings
  - `timeout` (Number) — Timeout(in seconds)
  - `followRedirects` (Checkbox) — Follow redirects
  - `failureMode` (StaticDropdown) — On Failure

---

## HTTP (OAuth2)

- **Package**: `@activepieces/piece-http-oauth2`
- **Auth**: OAuth2

### Actions

#### `send-oauth2-request` — Send an OAuth2 Request
Sends HTTP request to a specified URL that requires OAuth 2.0 authorization and returns the response.

**Props:**
  - `url` (ShortText, required) — URL
  - `method` (StaticDropdown, required) — Method
  - `headers` (Object) — Headers
  - `queryParams` (Object) — Query Parameters
  - `body_type` (StaticDropdown) — Body Type
  - `body` (DynamicProperties, required) — Body
  - `use_proxy` (Checkbox) — Use Proxy
  - `proxy_settings` (DynamicProperties, required) — Proxy Settings
  - `failsafe` (Checkbox) — No Error on Failure
  - `timeout` (Number) — Timeout (in seconds)

---

## Human Input

- **Package**: `@activepieces/piece-forms`
- **Auth**: None
- **Description**: Trigger a flow through human input.

### Triggers

#### `chat_submission` — Chat UI
Trigger the flow by sending a message

**Props:**
  - `about` (MarkDown) — about
  - `responseMarkdown` (MarkDown) — responseMarkdown
  - `botName` (ShortText, required) — Bot Name

#### `form_submission` — Web Form
Trigger the flow by submitting a form.

**Props:**
  - `about` (MarkDown) — about
  - `response` (MarkDown) — response
  - `waitForResponse` (Checkbox, required) — Wait for Response
  - `inputs` (Array, required) — Inputs
  - `displayName` (ShortText, required) — Field Name
  - `type` (StaticDropdown, required) — Field Type
  - `description` (ShortText) — Field Description
  - `required` (Checkbox, required) — Required

### Actions

#### `return_response` — Respond on UI
Return a file or text (markdown) as a response.

**Props:**
  - `markdown` (LongText) — Text (Markdown)
  - `file` (File) — Attachment

---

## Intercom

- **Package**: `@activepieces/piece-intercom`
- **Auth**: OAuth2
- **Description**: Customer messaging platform for sales, marketing, and support

### Triggers

#### `contactReplied` — Contact Replied
Triggers when a contact replies to a Conversation in Intercom.

**Props:**
  _(no props)_

#### `contact-updated` — Updated Contact
Triggers when a contact is updated.

**Props:**
  - `type` (StaticDropdown, required) — Type

#### `conversationAssigned` — Conversation assigned to any Intercom admin
Triggers when a conversation is assigned to an admin

**Props:**
  _(no props)_

#### `conversationClosed` — Conversation Closed
Triggers when a conversation is closed.

**Props:**
  _(no props)_

#### `conversationPartTagged` — Tag added to a conversation part
Triggers when a conversation part is tagged.

**Props:**
  _(no props)_

#### `conversationRated` — Conversation was rated
Triggers when a conversation is rated

**Props:**
  _(no props)_

#### `conversationSnoozed` — Conversation snoozed
Triggers when a conversation is snoozed

**Props:**
  _(no props)_

#### `conversationUnsnoozed` — Conversation unsnoozed
Triggers when a conversation is unsnoozed

**Props:**
  _(no props)_

#### `lead-added-email` — Lead Added Email
Triggers when a lead enters an email address.

**Props:**
  _(no props)_

#### `lead-converted-to-user` — Lead Converted To User
Triggers when a lead is converted to a user.

**Props:**
  _(no props)_

#### `new-company` — New Company
Triggers when a new company is created.

**Props:**
  _(no props)_

#### `newConversationFromUser` — New Conversation
Triggers when a conversation is created by a user or lead (not an admin).

**Props:**
  _(no props)_

#### `new-lead` — New Lead
Triggers when a new lead is created.

**Props:**
  _(no props)_

#### `new-ticket` — New Ticket
Triggers when a new ticket is created.

**Props:**
  _(no props)_

#### `new-user` — New User
Triggers when a new user is created.

**Props:**
  _(no props)_

#### `noteAddedToConversation` — Note added to conversation
Triggers when a note is added to a conversation

**Props:**
  - `keyword` (ShortText) — Keyword (optional)

#### `replyFromAdmin` — Reply from an Intercom admin
Triggers when a reply is received from an Intercom admin (not a user or lead)

**Props:**
  _(no props)_

#### `replyFromUser` — Reply from a user or lead
Triggers when a reply is received from a user or lead (not an admin)

**Props:**
  _(no props)_

#### `tag-added-to-lead` — Tag Added to Lead
Triggers when a tag is added to a lead.

**Props:**
  _(no props)_

#### `tag-added-to-user` — Tag Added to User
Triggers when a tag is added to a user.

**Props:**
  _(no props)_

### Actions

#### `addNoteToConversation` — Add note to conversation
Add a note (for other admins) to an existing conversation

**Props:**
  - `body` (ShortText, required) — Message Body

#### `add-note-to-user` — Add Note
Add a note to a user

**Props:**
  - `email` (ShortText, required) — Email
  - `body` (LongText, required) — Note Text

#### `add-or-remove-tag-on-company` — Add/Remove Tag on Company
Attach or remove a tag from a specific company.

**Props:**
  - `tagName` (ShortText, required) — Tag Name
  - `untag` (Checkbox) — Untag ?

#### `add-or-remove-tag-on-contact` — Add/Remove Tag on Contact
Attach or remove a tag from a specific contact.

**Props:**
  - `untag` (Checkbox) — Untag ?

#### `add-or-remove-tag-on-conversation` — Add/Remove Tag on Conversation
Attach or remove a tag from a specific conversation.

**Props:**
  - `untag` (Checkbox) — Untag ?

#### `create-article` — Create Article
Creates a new article in your Help Center.

**Props:**
  - `title` (LongText, required) — Title
  - `description` (ShortText) — Description
  - `body` (LongText, required) — Body
  - `state` (StaticDropdown, required) — State

#### `create-conversation` — Create Conversation
Creates a new conversation from a contact.

**Props:**
  - `contactType` (StaticDropdown, required) — Contact Type
  - `contactId` (Dropdown, required) — Contact ID
  - `body` (LongText, required) — Message Body

#### `create-ticket` — Create Ticket
Creates a new ticket.

**Props:**
  _(no props)_

#### `create-or-update-lead` — Create or Update Lead
Create or update an Intercom lead.If an ID is provided, the lead will be updated.

**Props:**
  - `leadId` (ShortText) — Lead ID
  - `name` (ShortText) — Full Name
  - `email` (ShortText) — Email
  - `phone` (ShortText) — Phone
  - `unsubscribe` (Checkbox) — Unsubscribed From Emails
  - `createdAt` (DateTime) — Created At
  - `customAttributes` (Object) — Custom Attributes

#### `create-or-update-user` — Create/Update User
Update a user within intercom given an email address.

**Props:**
  - `email` (ShortText, required) — Lookup Email
  - `name` (ShortText) — Full Name
  - `userId` (ShortText) — User ID
  - `phone` (ShortText) — Phone
  - `createdAt` (DateTime) — Created At
  - `customAttributes` (Object) — Custom Attributes

#### `create-user` — Create User
Creates a new user.

**Props:**
  - `email` (ShortText, required) — Email
  - `createdAt` (DateTime) — Created At
  - `userId` (ShortText) — User ID
  - `name` (ShortText) — Full Name
  - `customAttributes` (Object) — Custom Attributes

#### `find-company` — Find Company
Finds an existing company.

**Props:**
  - `searchField` (StaticDropdown, required) — Search Field
  - `searchValue` (ShortText, required) — Search Value

#### `find-conversation` — Find Conversation
Searches for conversations using various criteria

**Props:**
  - `searchField` (StaticDropdown, required) — Search Field
  - `matchType` (StaticDropdown, required) — Match Type
  - `searchTerm` (ShortText, required) — Search Term
  - `status` (StaticDropdown) — Status
  - `updateAfter` (DateTime) — Update After
  - `updateBefore` (DateTime) — Update Before

#### `find-lead` — Find Lead
Finds an existing lead.

**Props:**
  - `searchField` (StaticDropdown, required) — Search Field
  - `searchValue` (ShortText, required) — Search Value

#### `find-user` — Find User
Finds an existing user.

**Props:**
  - `searchField` (StaticDropdown, required) — Search Field
  - `searchValue` (ShortText, required) — Search Value

#### `get-conversation` — Retrieve a Conversation
Retrieves a specific conversation by ID.

**Props:**
  _(no props)_

#### `list-all-tags` — List Tags
List all tags.

**Props:**
  _(no props)_

#### `replyToConversation` — Reply to conversation
Reply (as an admin) to a conversation with a contact

**Props:**
  - `body` (ShortText, required) — Message Body

#### `send_message` — Send Message
Send a message to a contact (only allowed by admins)

**Props:**
  - `message_type` (StaticDropdown, required) — Message Type
  - `email_required_fields` (DynamicProperties, required) — Email Required Fields
  - `subject` (ShortText, required) — Subject
  - `template` (StaticDropdown, required) — Template
  - `body` (ShortText, required) — Message Body
  - `create_conversation_without_contact_reply` (Checkbox) — Create Conversation Without Contact Reply

#### `update-ticket` — Update Ticket
Updates an existing ticket.

**Props:**
  - `isOpen` (Checkbox) — Is Open
  - `state` (StaticDropdown) — State
  - `snoozedTill` (DateTime) — Snoozed Until

---

## JSON

- **Package**: `@activepieces/piece-json`
- **Auth**: SecretText
- **Description**: Convert JSON to text and vice versa

### Actions

#### `convert_json_to_text` — Convert Json to Text
Stringifies JSON.

**Props:**
  - `json` (Json, required) — JSON

#### `convert_text_to_json` — Convert Text to Json
Parses text into JSON.

**Props:**
  - `text` (LongText, required) — Text

---

## Linear

- **Package**: `@activepieces/piece-linear`
- **Auth**: SecretText
- **Description**: Issue tracking for modern software teams

### Triggers

#### `new_issue` — New Issue
Triggers when Linear receives a new issue

**Props:**
  _(no props)_

#### `removed_issue` — Removed Issue
Triggers when an existing Linear issue is removed

**Props:**
  _(no props)_

#### `updated_issue` — Updated Issue
Triggers when an existing Linear issue is updated

**Props:**
  _(no props)_

### Actions

#### `rawGraphqlQuery` — Raw GraphQL query
Perform a raw GraphQL query

**Props:**
  - `query` (LongText, required) — Query
  - `variables` (Object) — Parameters

---

## Math Helper

- **Package**: `@activepieces/piece-math-helper`
- **Auth**: None

### Actions

#### `addition_math` — Addition
Add the first number and the second number

**Props:**
  - `first_number` (Number, required) — First Number
  - `second_number` (Number, required) — Second Number

#### `division_math` — Division
Divide first number by the second number

**Props:**
  - `first_number` (Number, required) — First Number
  - `second_number` (Number, required) — Second Number

#### `generateRandom_math` — Generate Random Number
Generate random number between two numbers (inclusive)

**Props:**
  - `first_number` (Number, required) — First Number
  - `second_number` (Number, required) — Second Number

#### `modulo_math` — Modulo
Get the remainder of the first number divided by second number

**Props:**
  - `first_number` (Number, required) — First Number
  - `second_number` (Number, required) — Second Number

#### `multiplication_math` — Multiplication
Multiply first number by the second number

**Props:**
  - `first_number` (Number, required) — First Number
  - `second_number` (Number, required) — Second Number

#### `subtraction_math` — Subtraction
Subtract the first number from the second number

**Props:**
  - `first_number` (Number, required) — First Number
  - `second_number` (Number, required) — Second Number

---

## Metabase

- **Package**: `@activepieces/piece-metabase`
- **Auth**: CustomAuth
- **Description**: The simplest way to ask questions and learn from data

### Actions

#### `embedQuestion` — Embed question
Enable embedding for a Metabase question and configure parameters

**Props:**
  - `questionId` (ShortText, required) — Metabase question ID
  - `enableEmbedding` (Checkbox, required) — Enable embedding

#### `getDashboardQuestions` — Get Dashboard Questions
Execute all questions across all tabs in a Metabase dashboard and return their consolidated results in a single response

**Props:**
  - `dashboardId` (ShortText, required) — Metabase Dashboard ID
  - `parameters` (Object) — Parameters (slug name -> value)

#### `getGraphQuestion` — Get the graph of the question
Get the graph of a Metabase question and save it as a png

**Props:**
  - `questionId` (ShortText, required) — Metabase question ID
  - `parameters` (Object) — Parameters (slug name -> value)
  - `graphName` (ShortText) — The name of the graph (without the extension)
  - `waitTime` (Number, required) — Wait Time (seconds)

#### `getQuestionPngPreview` — Get Question PNG Preview
Get PNG preview rendering (low resolution) of a Metabase card/question.

**Props:**
  - `questionId` (ShortText, required) — Metabase question ID

#### `getQuestion` — Get question
Fetch the results of a Metabase question

**Props:**
  - `questionId` (ShortText, required) — Metabase question ID
  - `parameters` (Object) — Parameters (slug name -> value)

---

## Notion

- **Package**: `@activepieces/piece-notion`
- **Auth**: OAuth2
- **Description**: The all-in-one workspace

### Triggers

#### `new_comment` — New Comment
Triggers whenever someone adds a new comment to a specific Notion page. Perfect for notifications, review workflows, or automated responses to team feedback.

**Props:**
  _(no props)_

#### `new_database_item` — New Database Item
Triggers when an item is added to a database.

**Props:**
  _(no props)_

#### `updated_database_item` — Updated Database Item
Triggers when an item is updated in a database.

**Props:**
  _(no props)_

#### `updated_page` — Updated Page
Triggers whenever any page in your Notion workspace is modified or updated. Ideal for syncing content changes, backup processes, or notifying teams about documentation updates.

**Props:**
  _(no props)_

### Actions

#### `add_comment` — Add Comment
Add a comment to any Notion page to start discussions, provide feedback, or leave notes for team collaboration.

**Props:**
  - `comment_text` (LongText, required) — Comment Text

#### `append_to_page` — Append to Page
Appends content to the end of a page.

**Props:**
  - `content` (LongText, required) — Content

#### `archive_database_item` — Archive Database Item
Archive (soft-delete) a database item without permanently removing it. Archived items can be restored later if needed.

**Props:**
  _(no props)_

#### `create_database_item` — Create Database Item
Add a new item to a Notion database with custom field values and optional content. Ideal for creating tasks, records, or entries in structured databases.

**Props:**
  - `content` (LongText) — Content

#### `createPage` — Create Page
Create a new Notion page as a sub-page with custom title and content. Perfect for organizing documentation, notes, or creating structured page hierarchies.

**Props:**
  - `title` (ShortText) — Title
  - `content` (LongText) — Content

#### `notion-find-database-item` — Find Database Item
Searches for an item in database by field.

**Props:**
  _(no props)_

#### `find_page` — Find Page
Search for Notion pages by title with flexible matching options. Perfect for finding specific pages, building page references, or creating automated workflows based on page discovery.

**Props:**
  - `title` (ShortText, required) — Page Title
  - `exact_match` (Checkbox) — Exact Match
  - `limit` (Number) — Maximum Results

#### `get_page_comments` — Get Page Comments
Retrieve all comments from a Notion page, organized by discussion threads. Perfect for tracking feedback, managing reviews, or monitoring page discussions.

**Props:**
  _(no props)_

#### `getPageOrBlockChildren` — Get block content
Retrieve the actual content of a page (represented by blocks).

**Props:**
  - `parentId` (ShortText, required) — Page or parent block ID
  - `markdown` (Checkbox, required) — Markdown
  - `dynamic` (DynamicProperties, required) — Dynamic properties
  - `depth` (Number, required) — Depth

#### `restore_database_item` — Restore Database Item
Restore an archived database item back to active status. Perfect for recovering accidentally archived tasks, projects, or records.

**Props:**
  _(no props)_

#### `retrieve_database` — Retrieve Database Structure
Get detailed information about a Notion database including all its properties, field types, and configuration. Perfect for building dynamic forms, validation rules, or understanding database schemas.

**Props:**
  _(no props)_

#### `update_database_item` — Update Database Item
Update specific fields in a Notion database item. Perfect for maintaining data, tracking changes, or syncing information across systems.

**Props:**
  _(no props)_

---

## OpenAI

- **Package**: `@activepieces/piece-openai`
- **Auth**: SecretText
- **Description**: Use the many tools ChatGPT has to offer.

### Actions

#### `ask_assistant` — Ask Assistant
Ask a GPT assistant anything you want!

**Props:**
  - `assistant` (Dropdown, required) — Assistant
  - `prompt` (LongText, required) — Question
  - `memoryKey` (ShortText) — Memory Key

#### `extract-structured-data` — Extract Structured Data from Text
Returns structured data from provided unstructured text.

**Props:**
  - `model` (Dropdown, required) — Model
  - `text` (LongText, required) — Unstructured Text
  - `params` (Array, required) — Data Definition
  - `propName` (ShortText, required) — Name
  - `propDescription` (LongText) — Description
  - `propDataType` (StaticDropdown, required) — Data Type
  - `propIsRequired` (Checkbox, required) — Fail if Not present?

#### `generate_image` — Generate Image
Generate an image using text-to-image models

**Props:**
  - `model` (Dropdown, required) — Model
  - `prompt` (LongText, required) — Prompt
  - `resolution` (Dropdown) — Resolution
  - `quality` (Dropdown) — Quality

#### `ask_chatgpt` — Ask ChatGPT
Ask ChatGPT anything you want!

**Props:**
  - `model` (Dropdown, required) — Model
  - `prompt` (LongText, required) — Question
  - `temperature` (Number) — Temperature
  - `maxTokens` (Number, required) — Maximum Tokens
  - `topP` (Number) — Top P
  - `frequencyPenalty` (Number) — Frequency penalty
  - `presencePenalty` (Number) — Presence penalty
  - `memoryKey` (ShortText) — Memory Key
  - `roles` (Json) — Roles

#### `text_to_speech` — Text-to-Speech
Generate an audio recording from text

**Props:**
  - `text` (LongText, required) — Text
  - `model` (StaticDropdown, required) — Model
  - `speed` (Number) — Speed
  - `voice` (StaticDropdown, required) — Voice
  - `format` (StaticDropdown, required) — Output Format
  - `fileName` (ShortText) — File Name

#### `transcribe` — Transcribe Audio
Transcribe audio to text using whisper-1 model

**Props:**
  - `audio` (File, required) — Audio
  - `language` (StaticDropdown) — Language of the Audio

#### `translate` — Translate Audio
Translate audio to text using whisper-1 model

**Props:**
  - `audio` (File, required) — Audio

#### `vision_prompt` — Vision Prompt
Ask GPT a question about an image

**Props:**
  - `image` (File, required) — Image
  - `prompt` (LongText, required) — Question
  - `detail` (Dropdown) — Detail
  - `temperature` (Number) — Temperature
  - `maxTokens` (Number) — Maximum Tokens
  - `topP` (Number) — Top P
  - `frequencyPenalty` (Number) — Frequency penalty
  - `presencePenalty` (Number) — Presence penalty
  - `roles` (Json) — Roles

---

## PDF

- **Package**: `@activepieces/piece-pdf`
- **Auth**: None

### Actions

#### `convertToImage` — Convert to Image
Convert a PDF file or URL to an image

**Props:**
  - `file` (File, required) — PDF File or URL
  - `imageOutputType` (StaticDropdown, required) — Output Image Type

#### `extractPdfPages` — Extract PDF Pages
Extract or rearrange page(s)from PDF File.

**Props:**
  - `markdown` (MarkDown) — markdown
  - `file` (File, required) — PDF File or URL
  - `pageRanges` (Array) — Page Ranges
  - `startPage` (Number, required) — Start Page
  - `endPage` (Number, required) — End Page

#### `extractText` — Extract Text
Extract text from PDF file or url

**Props:**
  - `file` (File, required) — PDF File or URL

#### `imageToPdf` — Image to PDF
Convert image to PDF

**Props:**
  - `image` (File, required) — image

#### `mergePdfs` — Merge PDFs
Merges multiple PDF files into a single PDF document.

**Props:**
  - `pdfFiles` (Array, required) — PDF Files
  - `file` (File, required) — PDF File
  - `outputFileName` (ShortText) — Output File Name

#### `pdfPageCount` — PDF Page Count
Get page count of PDF file.

**Props:**
  - `file` (File, required) — PDF File or URL

#### `textToPdf` — Text to PDF
Convert text to PDF

**Props:**
  - `text` (LongText, required) — text

---

## Queue

- **Package**: `@activepieces/piece-queue`
- **Auth**: None
- **Description**: A piece that allows you to push items into a queue, providing a way to throttle requests or process data in a First-In-First-Out (FIFO) manner.

### Actions

#### `clear-queue` — Clear queue
Clears all items inside a queue

**Props:**
  - `info` (MarkDown) — info
  - `queueName` (ShortText, required) — Queue Name

#### `pull-from-queue` — Pull items from queue
Pull items from queue

**Props:**
  - `info` (MarkDown) — info
  - `queueName` (ShortText, required) — Queue Name
  - `numOfItems` (Number, required) — Number of items

#### `push-to-queue` — Push to Queue
Push item to queue

**Props:**
  - `info` (MarkDown) — info
  - `queueName` (ShortText, required) — Queue Name
  - `items` (Array, required) — Items

---

## Saleor

- **Package**: `@activepieces/piece-saleor`
- **Auth**: CustomAuth

### Actions

#### `addOrderNote` — Add Order Note
Add a note to a Saleor order

**Props:**
  - `orderId` (ShortText, required) — Order ID
  - `message` (LongText, required) — Note Message

#### `getOrder` — Get an order
Get an order according to the ID

**Props:**
  - `orderId` (ShortText, required) — The ID of the order

#### `rawGraphqlQuery` — Raw GraphQL query
Perform a raw GraphQL query

**Props:**
  - `query` (LongText, required) — Query
  - `variables` (Object) — Parameters

---

## Schedule

- **Package**: `@activepieces/piece-schedule`
- **Auth**: None
- **Description**: Trigger flow with fixed schedule

### Triggers

#### `cron_expression` — Cron Expression
Trigger based on cron expression

**Props:**
  - `cronExpression` (ShortText, required) — Cron Expression

#### `every_day` — Every Day
Triggers the current flow every day

**Props:**
  - `hour_of_the_day` (StaticDropdown, required) — Hour of the day
  - `run_on_weekends` (Checkbox, required) — Run on weekends (Sat,Sun)

#### `every_hour` — Every Hour
Triggers the current flow every hour

**Props:**
  - `run_on_weekends` (Checkbox, required) — Run on weekends (Sat,Sun)

#### `every_month` — Every Month
Triggers the current flow every month

**Props:**
  - `day_of_the_month` (StaticDropdown, required) — Day of the month
  - `hour_of_the_day` (StaticDropdown, required) — Hour of the day

#### `every_week` — Every Week
Triggers the current flow every week

**Props:**
  - `day_of_the_week` (StaticDropdown, required) — Day of the week
  - `hour_of_the_day` (StaticDropdown, required) — Hour of the day

#### `every_x_minutes` — Every X Minutes
Triggers the current flow every X minutes

**Props:**
  - `minutes` (StaticDropdown, required) — Minutes

---

## Slack

- **Package**: `@activepieces/piece-slack`
- **Auth**: OAuth2
- **Description**: Channel-based messaging platform

### Triggers

#### `channel_created` — Channel created
Triggers when a channel is created

**Props:**
  _(no props)_

#### `new-command-in-direct-message` — New Command in Direct Message
Triggers when a specific command is sent to the bot (e.g., @bot command arg1 arg2) via Direct Message.

**Props:**
  - `commands` (Array, required) — Commands
  - `ignoreBots` (Checkbox, required) — Ignore Bot Messages ?
  - `ignoreSelfMessages` (Checkbox, required) — Ignore Message from Yourself ?

#### `new_command` — New Command in Channel
Triggers when a specific command is sent to the bot (e.g., @bot command arg1 arg2)

**Props:**
  - `commands` (Array, required) — Commands
  - `channels` (MultiSelectDropdown) — Channels
  - `ignoreBots` (Checkbox, required) — Ignore Bot Messages ?

#### `new-direct-message` — New Direct Message
Triggers when a message was posted in a direct message channel.

**Props:**
  - `ignoreBots` (Checkbox, required) — Ignore Bot Messages ?
  - `ignoreSelfMessages` (Checkbox, required) — Ignore Message from Yourself ?

#### `new-mention-in-direct-message` — New Mention in Direct Message
Triggers when a username is mentioned in a direct message channel.

**Props:**
  - `ignoreBots` (Checkbox, required) — Ignore Bot Messages ?
  - `ignoreSelfMessages` (Checkbox, required) — Ignore Message from Yourself ?

#### `new_mention` — New Mention in Channel
Triggers when a username is mentioned.

**Props:**
  - `channels` (MultiSelectDropdown) — Channels
  - `ignoreBots` (Checkbox, required) — Ignore Bot Messages ?

#### `new-message-in-channel` — New Message Posted to Channel
Triggers when a new message is posted to a specific #channel you choose.

**Props:**
  - `ignoreBots` (Checkbox, required) — Ignore Bot Messages ?

#### `new-message` — New Public Message Posted Anywhere
Triggers when a new message is posted to any channel.

**Props:**
  - `ignoreBots` (Checkbox, required) — Ignore Bot Messages ?

#### `new_reaction_added` — New Reaction
Triggers when a new reaction is added to a message

**Props:**
  - `emojis` (Array) — Emojis (E.g fire, smile)
  - `channels` (MultiSelectDropdown) — Channels

#### `new-saved-message` — New Saved Message
Triggers when you save a message.

**Props:**
  _(no props)_

#### `new-team-custom-emoji` — New Team Custom Emoji
Triggers when a custom emoji has been added to a team.

**Props:**
  _(no props)_

#### `new-user` — New User
Triggers when a new user is created / first joins your org.

**Props:**
  _(no props)_

### Actions

#### `slack-add-reaction-to-message` — Add Reaction to Message
Add an emoji reaction to a message.

**Props:**
  - `ts` (ShortText, required) — Message Timestamp
  - `reaction` (ShortText, required) — Reaction (emoji) name

#### `slack-create-channel` — Create Channel
Creates a new channel.

**Props:**
  - `channelName` (ShortText, required) — Channel Name
  - `isPrivate` (Checkbox) — Is Private?

#### `delete-message` — Delete Message
Please provide the timestamp of the message you wish to retrieve, such as `1710304378.475129`. Alternatively, you can easily obtain the message link by clicking on the three dots next to the message and selecting the `Copy link` option.

**Props:**
  - `ts` (ShortText, required) — Message Timestamp

#### `slack-find-user-by-email` — Find User by Email
Finds a user by matching against their email address.

**Props:**
  - `email` (ShortText, required) — Email

#### `slack-find-user-by-handle` — Find User by Handle
Finds a user by matching against their Slack handle.

**Props:**
  - `handle` (ShortText, required) — Handle

#### `find-user-by-id` — Find User by ID
Finds a user by their ID.

**Props:**
  - `id` (ShortText, required) — ID

#### `getChannelHistory` — Get channel history
Retrieve all messages from a specific channel (

**Props:**
  - `oldest` (Number) — Oldest
  - `latest` (Number) — Latest
  - `inclusive` (Checkbox, required) — Inclusive
  - `includeAllMetadata` (Checkbox, required) — Include all metadata

#### `get-file` — Get File
Return information about a given file ID.

**Props:**
  - `fileId` (ShortText, required) — File ID

#### `get-message` — Get Message by Timestamp
Please provide the timestamp of the message you wish to retrieve, such as `1710304378.475129`. Alternatively, you can easily obtain the message link by clicking on the three dots next to the message and selecting the `Copy link` option.

**Props:**
  - `ts` (ShortText, required) — Message Timestamp

#### `invite-user-to-channel` — Invite User to Channel
Invites an existing User to an existing channel.

**Props:**
  _(no props)_

#### `listUsers` — List users
List all users of the workspace

**Props:**
  - `includeBots` (Checkbox, required) — Include bots?
  - `includeDisabled` (Checkbox, required) — Include disabled users?

#### `markdownToSlackFormat` — Markdown to Slack format
Convert Markdown-formatted text to Slack

**Props:**
  - `markdown` (LongText, required) — Markdown text

#### `request_action_direct_message` — Request Action from A User
Send a message to a user and wait until the user selects an action

**Props:**
  _(no props)_

#### `request_action_message` — Request Action in A Channel
Send a message in a channel and wait until an action is selected

**Props:**
  _(no props)_

#### `request_approval_direct_message` — Request Approval from A User
Send approval message to a user and then wait until the message is approved or disapproved

**Props:**
  _(no props)_

#### `request_approval_message` — Request Approval in a Channel
Send approval message to a channel and then wait until the message is approved or disapproved

**Props:**
  _(no props)_

#### `retrieveThreadMessages` — Retrieve Thread Messages
Retrieves thread messages by channel and thread timestamp.

**Props:**
  - `threadTs` (ShortText, required) — Thread ts

#### `searchMessages` — Search messages
Searches for messages matching a query

**Props:**
  - `query` (ShortText, required) — Search query

#### `send_direct_message` — Send Message To A User
Send message to a user

**Props:**
  - `unfurlLinks` (Checkbox) — Unfurl Links

#### `send_channel_message` — Send Message To A Channel
Send message to a channel

**Props:**
  - `text` (LongText) — Message
  - `sendAsBot` (Checkbox) — Send as a bot?
  - `file` (File) — Attachment
  - `replyBroadcast` (Checkbox) — Broadcast reply to channel
  - `unfurlLinks` (Checkbox) — Unfurl Links

#### `set-channel-topic` — Set Channel Topic
Sets the topic on a selected channel.

**Props:**
  - `topic` (LongText, required) — Topic

#### `slack-set-user-status` — Set User Status
Sets a user

**Props:**
  - `text` (ShortText, required) — Text
  - `emoji` (ShortText) — Emoji
  - `expiration` (Number) — Expires at

#### `updateMessage` — Update message
Update an existing message

**Props:**
  - `ts` (ShortText, required) — Message Timestamp
  - `text` (LongText, required) — Message

#### `slack-update-profile` — Update Profile
Update basic profile field such as name or title.

**Props:**
  - `firstName` (ShortText) — First Name
  - `lastName` (ShortText) — Last Name
  - `email` (ShortText) — Email
  - `userId` (ShortText) — User

#### `uploadFile` — Upload file
Upload file without sharing it to a channel or user

**Props:**
  - `file` (File, required) — Attachment
  - `title` (ShortText) — Title
  - `filename` (ShortText) — Filename

---

## SMTP

- **Package**: `@activepieces/piece-smtp`
- **Auth**: CustomAuth
- **Description**: Send emails using Simple Mail Transfer Protocol

### Actions

#### `send-email` — Send Email
Send an email using a custom SMTP server.

**Props:**
  - `from` (ShortText, required) — From Email
  - `senderName` (ShortText) — Sender Name
  - `to` (Array, required) — To
  - `cc` (Array) — CC
  - `replyTo` (ShortText) — Reply To
  - `bcc` (Array) — BCC
  - `subject` (ShortText, required) — Subject
  - `body_type` (StaticDropdown, required) — Body Type
  - `body` (LongText, required) — Body
  - `customHeaders` (Object) — Custom Headers
  - `attachments` (Array) — Attachments
  - `file` (File, required) — File
  - `name` (ShortText) — Attachment Name

---

## Snowflake

- **Package**: `@activepieces/piece-snowflake`
- **Auth**: CustomAuth
- **Description**: Data warehouse built for the cloud

### Actions

#### `insert-row` — Insert Row
Insert a row into a table.

**Props:**
  _(no props)_

#### `runMultipleQueries` — Run Multiple Queries
Run Multiple Queries

**Props:**
  - `sqlTexts` (Array, required) — SQL queries
  - `binds` (Array) — Parameters
  - `useTransaction` (Checkbox) — Use Transaction
  - `timeout` (Number) — Query timeout (ms)
  - `application` (ShortText) — Application name

#### `runQuery` — Run Query
Run Query

**Props:**
  - `sqlText` (ShortText, required) — SQL query
  - `binds` (Array) — Parameters
  - `timeout` (Number) — Query timeout (ms)
  - `application` (ShortText) — Application name

---

## Storage

- **Package**: `@activepieces/piece-store`
- **Auth**: None
- **Description**: Store or retrieve data from key/value database

### Actions

#### `add_to_list` — Add To List
Add Items to a list.

**Props:**
  - `key` (ShortText, required) — Key
  - `value` (Array, required) — Value
  - `ignore_if_exists` (Checkbox) — Ignore if value exists

#### `append` — Append
Append to a value in storage

**Props:**
  - `key` (ShortText, required) — Key
  - `value` (ShortText, required) — Value
  - `separator` (ShortText) — Separator

#### `get` — Get
Get a value from storage

**Props:**
  - `key` (ShortText, required) — Key
  - `defaultValue` (ShortText) — Default Value
  - `store_scope` (StaticDropdown, required) — Store Scope

#### `put` — Put
Put a value in storage

**Props:**
  - `key` (ShortText, required) — Key
  - `value` (ShortText, required) — Value

#### `remove_from_list` — Remove from List
Remove Item from a list

**Props:**
  - `key` (ShortText, required) — Key
  - `value` (ShortText, required) — Value

#### `remove_value` — Remove
Remove a value from storage

**Props:**
  - `key` (ShortText, required) — Key

---

## Sub Flows

- **Package**: `@activepieces/piece-subflows`
- **Auth**: None
- **Description**: Trigger and call another sub flow.

### Triggers

#### `callableFlow` — Callable Flow
Waiting to be triggered from another flow

**Props:**
  - `mode` (StaticDropdown, required) — Mode
  - `exampleData` (DynamicProperties, required) — Sample Data

### Actions

#### `callFlow` — Call Flow
Call a flow that has 

**Props:**
  - `mode` (StaticDropdown, required) — Mode
  - `flowProps` (DynamicProperties, required) — Payload
  - `waitForResponse` (Checkbox) — Wait for Response

#### `returnResponse` — Return Response
Return response to the original flow

**Props:**
  - `mode` (StaticDropdown, required) — Mode
  - `response` (DynamicProperties, required) — Response

---

## Tables

- **Package**: `@activepieces/piece-tables`
- **Auth**: None

### Triggers

#### `deletedRecord` — Record Deleted
Triggers when a record is deleted from the selected table.

**Props:**
  _(no props)_

#### `newRecord` — New Record Created
Triggers when a new record is added to the selected table.

**Props:**
  _(no props)_

#### `updatedRecord` — Record Updated
Triggers when a record is updated in the selected table.

**Props:**
  _(no props)_

### Actions

#### `tables-clear-table` — Clear Table
Delete all records from a table

**Props:**
  _(no props)_

#### `tables-create-records` — Create Record(s)
Insert one or more new records to a table.

**Props:**
  - `values` (DynamicProperties, required) — Records
  - `values` (Array, required) — Records

#### `tables-delete-record` — Delete Record(s)
Delete record(s) from a table

**Props:**
  - `records_ids` (Array, required) — Records IDs

#### `tables-find-records` — Find Records
Find records in a table with filters.

**Props:**
  - `limit` (Number) — Limit
  - `filters` (DynamicProperties) — Filters
  - `filters` (Array) — Filters
  - `filters` (Array) — Filters
  - `field` (StaticDropdown, required) — Field
  - `operator` (StaticDropdown, required) — Operator
  - `value` (ShortText, required) — Value

#### `tables-get-record` — Get Record
Get single record by its id.

**Props:**
  _(no props)_

#### `tables-update-record` — Update Record
Update values in an existing record

**Props:**
  - `values` (DynamicProperties, required) — Values

---

## Text Helper

- **Package**: `@activepieces/piece-text-helper`
- **Auth**: None
- **Description**: Tools for text processing

### Actions

#### `concat` — Concatenate
Concatenate two or more texts

**Props:**
  - `texts` (Array, required) — Texts
  - `separator` (ShortText) — Separator

#### `defaultValue` — Use Default Value if Input is Empty
Checks your input and returns the default value, if the input is an empty text or list

**Props:**
  - `value` (ShortText) — Enter value
  - `defaultString` (ShortText, required) — Default Value

#### `find` — Find
Find substring (Regex or Text).

**Props:**
  - `text` (ShortText, required) — text
  - `expression` (ShortText, required) — Expression

#### `html_to_markdown` — HTML to Markdown
Convert HTML to Markdown

**Props:**
  - `html` (LongText, required) — HTML Content

#### `json_to_ascii_table` — List to Text Table
Convert a list of items to a text table

**Props:**
  - `data` (Json, required) — List

#### `markdown_to_html` — Markdown to HTML
Convert markdown to HTML

**Props:**
  - `markdown` (LongText, required) — Markdown Content
  - `flavor` (StaticDropdown, required) — Flavor of Markdown
  - `headerLevelStart` (Number, required) — Minimum Header Level
  - `tables` (Checkbox, required) — Support Tables
  - `noHeaderId` (Checkbox, required) — No Header ID
  - `simpleLineBreaks` (Checkbox, required) — Simple Line Breaks
  - `openLinksInNewWindow` (Checkbox, required) — Open Links in New Window

#### `replace` — Replace
Replaces all instances of any word, character or phrase in text, with another.

**Props:**
  - `text` (ShortText, required) — Text
  - `searchValue` (ShortText, required) — Search Value
  - `replaceValue` (ShortText) — Replace Value
  - `replaceOnlyFirst` (Checkbox) — Replace Only First Match

#### `slugify` — Slugify
Slugifies strings.

**Props:**
  - `text` (ShortText, required) — Text

#### `split` — Split
Split a text by a delimiter

**Props:**
  - `text` (ShortText, required) — Text
  - `delimiter` (ShortText, required) — Delimiter

#### `stripHtml` — Remove HTML Tags
Removes every HTML tag and returns plain text

**Props:**
  - `html` (LongText, required) — HTML content

---

## Webhook

- **Package**: `@activepieces/piece-webhook`
- **Auth**: None
- **Description**: Receive HTTP requests and trigger flows using unique URLs.

### Triggers

#### `catch_webhook` — Catch Webhook
Receive incoming HTTP/webhooks using any HTTP method such as GET, POST, PUT, DELETE, etc.

**Props:**
  - `liveMarkdown` (MarkDown) — liveMarkdown
  - `syncMarkdown` (MarkDown) — syncMarkdown
  - `testMarkdown` (MarkDown, required) — Authentication
  - `authFields` (DynamicProperties) — Authentication Fields
  - `username` (ShortText, required) — Username
  - `password` (ShortText, required) — Password
  - `headerName` (ShortText, required) — Header Name
  - `headerValue` (ShortText, required) — Header Value

### Actions

#### `return_response_and_wait_for_next_webhook` — Respond and Wait for Next Webhook
return a response and wait for the next webhook to resume the flow

**Props:**
  - `markdown` (MarkDown) — markdown
  - `responseType` (StaticDropdown) — Response Type
  - `fields` (DynamicProperties, required) — Response

#### `return_response` — Return Response
return a response

**Props:**
  - `responseType` (StaticDropdown) — Response Type
  - `fields` (DynamicProperties, required) — Response
  - `respond` (StaticDropdown) — Flow Execution

---

