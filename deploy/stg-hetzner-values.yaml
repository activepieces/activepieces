# ============================================================================
# Activepieces — Hetzner-specific overrides (APP)
# Applied on top of prod-values.yaml:
#   helm upgrade --install activepieces deploy/activepieces-helm/ \
#     --values deploy/prod-values.yaml \
#     --values deploy/prod-hetzner-values.yaml
# ============================================================================

activepieces:
  frontendUrl: "https://stg.activepieces.dev"

postgresql:
  host: "10.0.1.4"

redis:
  host: "10.0.1.5"

s3:
  bucket: "activepiecesstagingbucket"
  region: "eu-central-1"
  endpoint: "https://fsn1.your-objectstorage.com"
  externalSecret:
    name: "activepieces-s3-secret"
    accessKeyIdKey: "accessKeyId"
    secretAccessKeyKey: "secretAccessKey"

ingress:
  hosts:
    - host: stg.activepieces.dev
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: activepieces-tls
      hosts:
        - stg.activepieces.dev
