---
title: 'Create Flow'
openapi: POST /v1/flows
---

<Info>
The `metadata` object accepts a reserved `embedConstraints` key that lets embedded flows require a specific piece before publishing and lock the trigger. See [Publish Constraints](/embedding/publish-constraints) for the full schema.
</Info>