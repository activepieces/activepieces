---
title: "Getting Started"
description: ""
---

Let's explain the the terminology and structure we are going to use to build custom piece for third party api.

* `Piece`:
* `Config`:
* `Trigger`:
* `Action`:

All pieces are belong to the package apps

In the following example we are going to build an piece for `GitHub`, with `New Issue Trigger` and `Create Issue Action`.

