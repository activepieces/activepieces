---
title: 'Dev Docker Compose (Recommended)'
description: ''
---

The `docker-compose.dev.yml` file is a Docker Compose file specifically used for development environments. 

This file includes the necessary configurations for running a development version of your application.

Here are the steps to use `docker-compose.dev.yml`:

1. Run the script to install node_modules 
```bash
sh tools/setup-dev.sh
```

2. Run the following command to start postgres & redis:

```bash
docker compose -f docker-compose.dev.yml up
```

3. Run the backend & frontend
```
npm run start:native
```

4. Go to ***localhost:4200*** on your web browser and sign in with these details:

Email: `dev@ap.com`
Password: `12345678`
